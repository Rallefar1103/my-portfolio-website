class Project {
  constructor(id, title, description, imgUrl, techStack) {
    this.id = id;
    this.title = title;
    this.description = description;
    this.imgUrl = imgUrl;
    this.techStack = techStack;
  }
}

export default Project;
