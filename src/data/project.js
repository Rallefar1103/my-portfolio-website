class Project {
  constructor(id, title, description, imgUrl, techStack, archUrl) {
    this.id = id;
    this.title = title;
    this.description = description;
    this.imgUrl = imgUrl;
    this.techStack = techStack;
    this.archUrl = archUrl;
  }
}

export default Project;
